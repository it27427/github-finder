import { useContext } from 'react';
import AlertContext from '@/context/alert/AlertContext';

const Alert = () => {
  return (
    <div>
      <h1>Alert</h1>
    </div>
  );
};

export default Alert;
